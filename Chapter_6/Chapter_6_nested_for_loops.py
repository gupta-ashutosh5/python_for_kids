# Declare a list.
hugehairypants = ['huge', 'hairy', 'pants']
count = 0
for i in hugehairypants:
  count = count + 1
  print(count) # print all elements of the list.
  for j in hugehairypants:
    print(j) # print whole list 3 times.

# 1st call 1 - [huge, hairy, list]. Same happens for 2 and 3.
